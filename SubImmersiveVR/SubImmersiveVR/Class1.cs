using System;
using System.Collections.Generic;
using System.Linq;
using Harmony;

namespace SubImmersiveVR
{
    class Stuff
    {
        //public static IEnumerable<CodeInstruction> TranspilerLogger(IEnumerable<CodeInstruction> instructions)
        //{
        //    var l = XTranspiler(instructions).ToList(); // name your actual transpiler XTranspiler
        //    string s = "Code:";
        //    int i = 0;
        //    foreach (var c in l)
        //    {
        //        s += "\n" + i + ": " + c;
        //        i++;
        //        yield return c;
        //    }
        //    Console.WriteLine(s); // or just print the entire thing out to copy to a text editor.
        //}
    }
}
